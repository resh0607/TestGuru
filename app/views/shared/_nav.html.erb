<%= link_to 'Test Guru', root_path %>
<% if user_signed_in? %>
  <p class="nav user"><%= t('.welcome') %> <%= current_user.name %> Guru | <%= link_to t('.logout'), destroy_user_session_path, method: :delete %></p>
<% else %>
  <p class="nav user"><%= link_to t('.login'), new_user_session_path %> | <%= link_to t('.create_account'), new_user_registration_path %></p>
<% end %>